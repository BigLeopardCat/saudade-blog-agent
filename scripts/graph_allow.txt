# 受控允许清单（20260916）——兜住"词性没问题、但被每篇词配额按 tf·idf 挤掉"的主题词
#
# 入选标准（三条全满足才写进来）：
#   1. 语料确有出现，且是本站主题词（不是泛化动词/修辞词）；
#   2. 它是访客会真的拿去搜的词（前端检索框输入后应该能定位到）；
#   3. 语料出现次数 ≥ 3（1~2 次的词进图只会变成孤点，标签读者也认不出语境）。
#
# 逐词实测（20260916 语料 6 篇，出现次数 / 当前是否节点）：
#   部署    13 次  否（被配额挤掉）
#   缓存     8 次  否
#   编译     6 次  否
#   客户端   5 次  否
#   数据库   3 次  否
#   ---- 以下**故意不收** ----
#   索引 / 向量 / 图谱 / 检索 / 框架：语料出现 **0 次**（它们在 docs/word-graph.md 和
#     展示柜代码里，而建图语料只取"线上公开文章"）——硬塞进去就是凭空造词，
#     图上会多出几个文章里根本没提过的点。
#   接口(2) / 容器(1) / 调试(1) / 异步(已在图中)：出现次数太少，孤点，不入。
#   并发 / 模块 / 变量 / 服务端 / 线程 / 内存 / 协程：**已经在图里**，不必列（列了也无害，
#     但清单要能一眼看完，多余项会让人误以为它们曾经缺失）。
#
# 机制：清单里的词不参与 tf·idf 竞争，直接进候选词表（见 select_vocab 的 allow 分支）；
# 仍要过 embedding 与质量门，木有问题才出图。改这里同样先 `--dry-run` 复验词表报告。
#
# 格式：每行一个词，# 后为注释，一行可写多个（空格/逗号分隔）

# ── 主清单：被每篇 tf·idf 配额挤掉的主题词（20260916 实测名单）──
部署 缓存 编译 客户端 数据库

# ── 回补：加 userdict 后**连带掉出**的词（不是有意剔除，故此补齐）──
# userdict 让「前端/后端/本地」不再碎成 前/f+端/v 之类，词频统计随之位移，于是有词在
# 配额边界上被挤出去。对比 dry-run 前后两份词表报告（013021 vs 013108）实测掉出这两个：
#   初始化  6 次（id19:1 id16:5）—— 真主题词（embedding/浏览器/agent 初始化），必须回补
#   note    4 次（id22:1 id19:3）—— 本站领域名词（文章=note，API 路径 noteKey/notes 都带它），回补
# 加这两词 ⇒ 词表净增 8、零丢失（333 → 341），改动只增不减。
初始化 note
