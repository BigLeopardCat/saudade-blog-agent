# jieba 用户词典（20260916）——只治"切分/词性误伤"，不改 POS_DROP 本体
#
# 为什么需要它（逐词实测，20260916 语料 6 篇）：
#   前端 → f（方位词）：命中 POS_DROP 的 f 被丢；语料出现 38 次
#   后端 → 被切成 后/f + 端/v：两片都过不了长度/词性闸；语料出现 17 次
#   本地 → r（代词）：命中 POS_DROP 的 r 被丢；语料出现 19 次
# 三个词都是本站高频领域词（前端/后端/本地 vs 线上/远程 这类区分在文章里反复出现），
# 却是"查不到"的：中文 query 走本地兜底定位时永远零命中。
#
# 纪律：这里是**切分与词性**的白名单，不是"想让谁进图就写谁"。
#   - 只收「jieba 切错/标错导致被闸掉」的词；被每篇 tf·idf 配额挤掉的走 graph_allow.txt；
#   - 加词前先实测语料出现次数（0 次的绝对不加，见 graph_allow.txt 的纪律说明）；
#   - 词性标签必须选**非** POS_DROP 的真实词性：前端/后端是名词 n；本地在此处是
#     「本地缓存/本地兜底」的限定用法，同样按 n 收（标 s/f/r 都会被闸，等于没加）。
#
# 改本文件后：`--dry-run` 看词表报告里这三个词是否出现（应出现），再正式重跑。
# 格式：词 [词频] [词性]
前端 200 n
后端 200 n
本地 200 n
